springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

app:
  ai:
    mode: ${AI_MODE:mock}
  time:
    storage-zone: UTC
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:8081,http://localhost:19006}
