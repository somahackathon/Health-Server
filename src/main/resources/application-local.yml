springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

app:
  ai:
    mode: ${AI_MODE:mock}
    fitness-mode: ${AI_FITNESS_MODE:${AI_MODE:mock}}
    posture-mode: ${AI_POSTURE_MODE:${AI_MODE:mock}}
  time:
    storage-zone: UTC
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:8081,http://localhost:19006}
