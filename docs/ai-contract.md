# AI Contract

This document tracks the expected JSON boundary between the Spring server and AI servers. Items marked TODO are not final and must be agreed with the AI team before implementation.

## Common Headers

- `X-Correlation-Id`: Required for request tracing. Generated by the Spring server if the client does not provide one.
- `Idempotency-Key`: TODO. Decide whether analysis creation requires client-provided or server-generated idempotency keys.

## Fitness Analysis Request

The PAPS evaluation API response is the current stable input candidate for future fitness AI requests. The AI team should expect item-level grades, standard version metadata, calculated BMI, and completeness fields from the Spring server. Evaluation results are fitness-management reference information, not medical diagnosis.

```json
{
  "correlationId": "TODO",
  "modelVersion": "TODO",
  "profile": {
    "birthDate": "TODO",
    "gender": "TODO",
    "heightCm": 0,
    "weightKg": 0
  },
  "records": [
    {
      "itemCode": "TODO",
      "value": 0,
      "unit": "TODO",
      "measuredAt": "TODO"
    }
  ]
}
```

## Fitness Analysis Response

```json
{
  "correlationId": "TODO",
  "modelVersion": "TODO",
  "summary": "TODO",
  "recommendations": [
    {
      "title": "TODO",
      "description": "TODO"
    }
  ]
}
```

## Posture Analysis Request

```json
{
  "correlationId": "TODO",
  "modelVersion": "TODO",
  "exerciseType": "TODO",
  "video": {
    "contentType": "TODO",
    "sizeBytes": 0
  }
}
```

Actual transport for video bytes is TODO. Options include multipart upload from Spring to AI or pre-agreed binary streaming.

## Posture Analysis Response

```json
{
  "correlationId": "TODO",
  "modelVersion": "TODO",
  "status": "COMPLETED",
  "feedback": [
    {
      "code": "TODO",
      "message": "TODO",
      "severity": "TODO"
    }
  ]
}
```

## Analysis Job Status

Initial status candidates:

- `PENDING`
- `PROCESSING`
- `COMPLETED`
- `FAILED`
- `EXPIRED`

TODO: Confirm whether posture analysis is synchronous, asynchronous, or hybrid.

The Spring server stores `AiAnalysisJob` rows for temporary status/result lookup. Request and result payloads are not long-term user history and must be deleted after expiration.

## Common Error Format

```json
{
  "correlationId": "TODO",
  "code": "TODO",
  "message": "TODO",
  "retryable": false
}
```

## Timeout And Retry

- The Spring server should use finite connection and read timeouts.
- Retry only idempotent requests or requests protected by an idempotency key.
- Do not retry video uploads blindly without an agreed duplicate-handling strategy.

## Video Storage Rule

The Spring server and AI server must not persist raw videos beyond the analysis lifecycle. The Spring server deletes temporary videos after success or failure.

## Open Questions

- Final AI endpoint paths and HTTP methods.
- Fitness and posture model version format.
- Gender and PAPS item code enums.
- Whether fitness AI requires all five PAPS components or can accept partial evaluation results.
- Final mapping from PAPS evaluation response fields to the AI fitness request payload.
- Request and response field validation ranges.
- Synchronous versus asynchronous posture analysis.
- Timeout values and retry count.
- Error code catalog.
- Whether AI stores any derived posture artifacts.

